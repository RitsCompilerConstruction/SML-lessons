(*
  ``mlton 1_standard_io.sml`` でコンパイル、
  ``./1_standard_io`` で実行できます。

  print 関数は引数を 1 つ取る関数で、unit 値 ( 意味を持たない値 ) を返します。
  1 つ目の引数は string ( 文字列 ) 型でなければなりません。
*)

print "hello, ";
print "world";

(*
  [課題 1] 上のプログラムを、w という文字の直前で改行して出力するように改変してください。
*)
